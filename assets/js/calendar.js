$(document).ready(function() {
    $("form.no-refresh").submit(function(e){
        e.preventDefault(); // Evitamos el resfresco del navegador cuando se le de click al boton submit del formulario.
    });
    
    function ValidarReserva(){
        
    }
    
});